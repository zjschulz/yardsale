class AppContainer {
    shops = []
    items = []
    url = 'http://localhost:3000'

    getItems() {
        // fetch to /items
        // populate the items and shops properties with the returned data
        // renderItems
    }

    renderItems() {
        // create DOM nodes and insert data into them to render in DOM
    }

}